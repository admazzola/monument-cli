import Phaser from 'phaser'

export default class  {

  constructor (game) {
   
    this.game = game;
    this.init();
  }

  init()
  {

    /*  this.game.load.tilemap('tilemap', '/images/maps/firstMap.json', null, Phaser.Tilemap.TILED_JSON);


      map = this.game.add.tilemap('tilemap');
      map.addTilesetImage('tiles', 'tiles');

      layer = map.createLayer('MyTerrain');
      layer.resizeWorld();
      layer.wrap = true;
      */
  }

  //new a menu that allows for picking this
  loadTerrainFile()
  {

  }

  update () {

  }


}
